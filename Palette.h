#include <SFML/Graphics.hpp>

//Explication
namespace Palette{
     //--Fond & Interface
     const sf::Color FondSombre(30, 32, 40); //Foond style arcade
     const sf::Color BordureOr(235, 195, 110);//Contour style bouton doré(geo)
     const sf::Color FondBoutonGris(210, 215, 220); //Bouton metallique clair

    //---Couleur Elementaire GENSHIN IMPACT 5VERSION PASTEL) ---
    const sf::Color AnemoPastel(150, 225, 205); //vert turquoise doux
    const sf::Color GeoPastel(245, 210, 120); //Jaune ambre doux
    const sf::Color ElectroPastel(190, 160, 220);//violet lavande d'Inazuma
    const sf::Color DendroPastel(165, 220, 140);// vert prairie doux
    const sf::Color HydroPastel(135, 195, 240);// Bleu ciel / Ocean
    const sf::Color PyroPastel(245, 140, 130);// rouge/coraile doux
    const sf::Color CryoPastel(175, 230, 245);//Bleu givre tres clair

    //-- ASSIGNATION POUR LES LETTRES DU LOGO TETRIS
    const sf::Color Lettre_T1 = PyroPastel;
    const sf::Color Lettre_E = HydroPastel;
    const sf::Color Lettre_T2 = PyroPastel;
    const sf::Color Lettre_R = DendroPastel;
    const sf::Color Lettre_I = AnemoPastel;
    const sf::Color Lettre_S = GeoPastel;
}
